import "./CartWidget.css";

const CartWidget = () => (
  <div className="shopping_cart">
    <i className="large material-icons shopping_cart_icon">shopping_cart</i>
  </div>
);

export default CartWidget;
